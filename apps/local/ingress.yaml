apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: library
  namespace: library
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: library
                port:
                  name: http
